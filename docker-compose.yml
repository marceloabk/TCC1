version: '3'

services:
    web:
        build: .
        command: npm start
        volumes:
            - .:/usr/src/app
            - /usr/src/app/node_modules/
    ira:
        build: .
        command: npm start
        volumes:
            - ./ira:/usr/src/app
            - /usr/src/app/node_modules/
    db:
        image: rethinkdb
        volumes:
            - ./data:/data
        ports:
            - 8080:8080
            - 28015:28015
            - 29015:29015
